import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.nexttodoapp',
  appName: 'next-todo-app',
  webDir: 'public'
};

export default config;
